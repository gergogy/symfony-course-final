{% extends '@Front/layout.html.twig' %}

{% block content %}
    <h1>Job creation</h1>

    {{ form_start(form, { action: path('jobz_front_job_new') }) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('jobz_front_frontcontroller_home') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
